# دستیار مولد «فرزند من»

## وضعیت

دستیار مولد عمومی و دستیار پنل مدیریت به‌صورت مستقل برای پروژه «فرزند من» پیاده‌سازی شده‌اند. دیتابیس، Mistral API Key، CORS، دانش و Rate Limit این پروژه از زینالیکید جدا هستند.

## دستیار عمومی

- پاسخ مولد فقط براساس دانش عمومی منتشرشده فرزند من
- دکمه انتقال فقط به مسیرهای عمومی تأییدشده
- پیشنهاد سؤال بعدی بعد از هر پاسخ
- پاسخ دوستانه و پیشنهادهای پیش‌فرض برای سؤال نامرتبط
- جلوگیری سروری از پاسخ درباره پنل مدیریت
- حداکثر ۱۰ سؤال در ۳ دقیقه و ۱۰ سؤال در روز برای هر مرورگر
- بدون بازخورد بله/خیر و بدون پنجره رضایت در رابط دستیار

## دستیار پنل مدیریت

- دکمه کوچک SVG شخص هدفون‌دار در تمام تب‌های پنل
- فقط با نشست معتبر مدیر
- دانش مدیریتی در جدول مستقل
- حداکثر ۱۰ سؤال در هر دو دقیقه
- دکمه انتقال به تب و بخش دقیق پنل
- عدم نمایش یا دسترسی دانش مدیریتی برای کاربران عمومی

## مدیریت دانش

تب «دستیار» شامل دو بخش مستقل است:

- دانش عمومی سایت
- راهنمای پنل مدیریت

FAQها، دوره‌های فعال، آموزش‌های عمومی و محصولات فعال فقط از خروجی عمومی همین پروژه وارد Knowledge Base شده‌اند. هیچ Submission، اطلاعات بانکی، Secret یا داده خصوصی وارد دانش نشده است.

## امنیت

- `assistant_knowledge` و `assistant_admin_knowledge` دارای RLS و منع صریح مرورگر هستند.
- API عمومی هیچ Query به دانش مدیریتی ندارد.
- Browser مستقیماً با Mistral تماس نمی‌گیرد.
- Mistral API Key فقط در Supabase Edge Secrets پروژه فرزند من است.
- Origin زینالیکید در Edge Functions فرزند من پذیرفته نمی‌شود.
